<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	
	<select id="login" parameterType="memberVO" resultType="memberVO">
		select * from member where id = #{id} and pw = #{pw}
	</select>
	
	<select id="searchOne" parameterType="String" resultType="memberVO">
		select * from member where id = #{id};
	</select>
	
	<update id="logoutTimeUpdate" parameterType="String">
		update member set logout_time = current_timestamp where id = #{id}
	</update>
	
	<select id="purchsList" parameterType="String" resultType="orderVO"> select * from tb_order where buyer_id = #{id} </select>
<!--   05-1 23:03 add  -->
<select id="sellList" parameterType="String" resultType="orderVO"> select * from tb_order where seller_id = #{id} </select>
</mapper>











